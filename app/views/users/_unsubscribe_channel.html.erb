<%= form_for(chan.active_subscriptions.find_by(subscribed_id: @user.id),
             html: { method: :delete }) do |f| %>
  <div><%= hidden_field_tag :subscriber_id, chan.id %></div>
  <%= f.submit "Unsubscribe", class: "btn btn-unsubscribe" %>
<% end %>
<li>
  <%= link_to image_tag(user.avatar.url(:thumb), size: "80x80", alt: "Photo") %>
  <span><%= link_to "<h4><span>#{user.full_name} </span><span>(#{user.username})</span></h4>".html_safe, user %></span>
</li>
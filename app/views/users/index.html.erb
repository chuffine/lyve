<% provide(:title, 'All users') %>
<div class="container">
  <div class="row">
<h1>All users</h1>
<div class="col-xs-12">
<%= will_paginate %>

<ul class="users-index">
  <%= render @users %>
</ul>

<%= will_paginate %>
  </div>
  </div>
</div>